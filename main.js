let number = +prompt('Введите число')
let power = +prompt('Введите степень')

let result = 1

for (let i = 0; i < power; i++) {
  if (isNaN(number) && 0(number) || isNaN(power) && 0(power)) {
    number = 2
    power = 2
  }

  result *= number
}

alert(result)
